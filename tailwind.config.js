/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js}",
    "./*.html"
  ],
  theme: {
    extend: {
      colors: {
        // We can add custom colors here later
      },
      fontFamily: {
        // We can add custom fonts here later
      },
    },
  },
  plugins: [],
} 